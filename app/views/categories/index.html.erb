<p style="color: green"><%= notice %></p>

<h1>Categories</h1>

<div id="categories">
  <% @categories.each do |category| %>
    <%= render category %>
    <a href="<%= category_path(category) %>">Brows Products</a>
  <% end %>
</div>

<%= link_to "New category", new_category_path %>
